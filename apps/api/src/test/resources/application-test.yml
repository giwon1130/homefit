spring:
  datasource:
    url: jdbc:tc:postgresql:16:///homefit_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  flyway:
    enabled: false
  data:
    redis:
      # Lettuce 는 lazy connect — 실제 호출 시점에만 접속 시도. contextLoads 는 통과.
      host: localhost
      port: 6379
